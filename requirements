# get into the file directory from terminal > lusmart-
# run "pip install -r requirements.txt" to make env
# then run ".\env\Scripts\activate"
# then run the "python app.py"

python -m venv env
.\env\Scripts\activate

Flask==2.3.3
opencv-contrib-python==4.10.0.84
numpy==2.1.3
firebase-admin=5.2.0